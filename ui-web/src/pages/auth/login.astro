---
import Layout from "../../layouts/Layout.astro";
---

<Layout title="Login - MazeChase">
    <div class="min-h-screen flex items-center justify-center p-4">
        <div class="flex flex-col items-center w-full max-w-md">
            <h1 class="text-yellow-400 text-5xl font-bold mb-2 text-center tracking-wider drop-shadow-lg">
                🎮 MazeChase
            </h1>
            <p class="text-gray-400 mb-8">Log in om te spelen</p>

            <div class="bg-gray-900 bg-opacity-95 border-4 border-gray-400 rounded-lg p-8 w-full shadow-2xl backdrop-blur-sm">
                <form id="login-form" class="space-y-6">
                    <div class="space-y-2">
                        <label class="block text-gray-300 text-xl font-bold text-center" for="username">
                            Gebruikersnaam
                        </label>
                        <input
                                class="w-full px-4 py-3 text-lg font-semibold text-center border-2 border-gray-400 rounded bg-gray-400 text-black focus:outline-none focus:border-gray-500 focus:ring-2 focus:ring-gray-500"
                                type="text"
                                id="username"
                                name="username"
                                maxlength="50"
                                required
                        >
                    </div>

                    <div class="space-y-2">
                        <label class="block text-gray-300 text-xl font-bold text-center" for="password">
                            Wachtwoord
                        </label>
                        <input
                                class="w-full px-4 py-3 text-lg font-semibold text-center border-2 border-gray-400 rounded bg-gray-400 text-black focus:outline-none focus:border-gray-500 focus:ring-2 focus:ring-gray-500"
                                type="password"
                                id="password"
                                name="password"
                                maxlength="50"
                                required
                        >
                    </div>

                    <div class="pt-4">
                        <button
                                type="submit"
                                id="login-btn"
                                class="w-full bg-green-600 text-white border-4 border-green-400 text-2xl font-bold py-4 px-8 rounded hover:bg-green-500 transition-all duration-300 hover:scale-105 hover:shadow-lg disabled:opacity-50 disabled:cursor-not-allowed disabled:hover:scale-100"
                        >
                            <span id="login-text">INLOGGEN</span>
                            <span id="login-spinner" class="hidden">
                                <svg class="animate-spin h-6 w-6 mx-auto" fill="none" viewBox="0 0 24 24">
                                    <circle class="opacity-25" cx="12" cy="12" r="10" stroke="currentColor"
                                            stroke-width="4"></circle>
                                    <path class="opacity-75" fill="currentColor"
                                          d="M4 12a8 8 0 018-8V0C5.373 0 0 5.373 0 12h4zm2 5.291A7.962 7.962 0 014 12H0c0 3.042 1.135 5.824 3 7.938l3-2.647z"></path>
                                </svg>
                            </span>
                        </button>
                    </div>
                </form>

                <div class="mt-6 text-center">
                    <p class="text-gray-400 mb-3">Nog geen account?</p>
                    <a href="/auth/register">
                        <button
                                type="button"
                                class="w-full bg-blue-600 text-white border-4 border-blue-400 text-xl font-bold py-3 px-8 rounded hover:bg-blue-500 transition-all duration-300 hover:scale-105"
                        >
                            REGISTREREN
                        </button>
                    </a>
                </div>

                <div class="mt-4 text-center">
                    <a href="/" class="text-gray-500 hover:text-gray-300 text-sm">← Terug naar home</a>
                </div>
            </div>
        </div>
    </div>
</Layout>

<script>
    import {login} from "../../lib/auth";

    let isLoginLoading = false;

    const setLoginLoading = (loading: boolean) => {
        isLoginLoading = loading;
        const btn = document.getElementById('login-btn') as HTMLButtonElement;
        const text = document.getElementById('login-text');
        const spinner = document.getElementById('login-spinner');
        const usernameInput = document.getElementById('username') as HTMLInputElement;
        const passwordInput = document.getElementById('password') as HTMLInputElement;

        if (btn && text && spinner && usernameInput && passwordInput) {
            btn.disabled = loading;
            usernameInput.disabled = loading;
            passwordInput.disabled = loading;

            if (loading) {
                text.classList.add('hidden');
                spinner.classList.remove('hidden');
            } else {
                text.classList.remove('hidden');
                spinner.classList.add('hidden');
            }
        }
    };

    const onLogin = async (e: Event) => {
        e.preventDefault();

        if (isLoginLoading) return;
        setLoginLoading(true);

        const usernameInput = document.getElementById('username') as HTMLInputElement;
        const passwordInput = document.getElementById('password') as HTMLInputElement;

        if (!usernameInput || !passwordInput) {
            alert('Formulier niet gevonden');
            setLoginLoading(false);
            return;
        }

        const username = usernameInput.value.trim();
        const password = passwordInput.value.trim();

        if (!username || !password) {
            alert('Vul gebruikersnaam en wachtwoord in');
            setLoginLoading(false);
            return;
        }

        const {err} = await login(username, password);

        if (err) {
            alert('Login mislukt: ' + err);
            setLoginLoading(false);
            return;
        }
        
        // Small delay to ensure cookie is set before redirect
        setTimeout(() => {
            window.location.href = "/lobby";
        }, 100);
    };

    document.addEventListener('DOMContentLoaded', () => {
        const form = document.getElementById('login-form');
        if (form) {
            form.addEventListener('submit', onLogin);
        }
    });
</script>
